---
phase: 15-test-fix-and-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/gateway/index.test.ts
  - src/__tests__/gateway/adapter-wiring.test.ts
  - src/__tests__/gateway/resume.test.ts
  - src/__tests__/gateway/session-map.test.ts
  - src/__tests__/gateway/normalizer.test.ts
  - src/__tests__/governance/usage-tracker.test.ts
  - src/__tests__/governance/model-router.test.ts
  - src/__tests__/governance/budget-engine.test.ts
  - src/__tests__/governance/watchdog.test.ts
  - src/__tests__/governance/telemetry.test.ts
  - src/__tests__/escalation/status.test.ts
  - src/__tests__/escalation/notifications.test.ts
  - src/__tests__/escalation/handoff.test.ts
  - src/__tests__/escalation/pause.test.ts
  - src/__tests__/policy/wiring.test.ts
  - src/__tests__/policy/audit-log.test.ts
  - src/__tests__/policy/channel-policies.test.ts
  - src/__tests__/policy/engine.test.ts
  - src/__tests__/policy/skill-overlays.test.ts
  - src/__tests__/policy/approval-queue.test.ts
  - src/__tests__/orchestrator/executor.test.ts
  - src/__tests__/orchestrator/workflow-state.test.ts
  - src/__tests__/orchestrator/task-graph.test.ts
  - src/__tests__/orchestrator/telemetry.test.ts
  - src/__tests__/orchestrator/resumable-jobs.test.ts
  - src/__tests__/orchestrator/governance-adapter.test.ts
  - src/__tests__/event-processor.test.ts
  - src/__tests__/event-log.test.ts
  - src/__tests__/retry-queue.test.ts
autonomous: true
requirements:
  - TEST-01: All pre-existing test failures diagnosed and categorized
  - TEST-02: Gateway test failures fixed (7+ tests passing)
  - TEST-03: Governance test failures fixed (12+ tests passing)
  - TEST-04: Escalation/Policy test failures fixed (6+ tests passing)
  - TEST-05: All remaining test failures fixed
  - TEST-06: Full test suite passes with >95% pass rate

must_haves:
  truths:
    - "All gateway tests pass (session mapping, event processing, threading)"
    - "All governance tests pass (usage tracking, budget, watchdog, model router)"
    - "All escalation tests pass (pause, handoff, notifications, status)"
    - "All policy tests pass (engine, channel policies, skill overlays, audit log)"
    - "All orchestrator tests pass (executor, workflow state, task graph)"
    - "Overall test pass rate exceeds 95% (555+/577 tests)"
  artifacts:
    - path: "src/__tests__/gateway/index.test.ts"
      provides: "Gateway event processing tests"
    - path: "src/__tests__/governance/usage-tracker.test.ts"
      provides: "Governance usage tracking tests"
    - path: "src/__tests__/escalation/status.test.ts"
      provides: "Escalation status tests"
  key_links:
    - from: "Gateway tests"
      to: "session-map.ts, index.ts, resume.ts"
      via: "shared state isolation"
    - from: "Governance tests"
      to: "usage-tracker.ts, budget-engine.ts, watchdog.ts"
      via: "file system state"
    - from: "Escalation tests"
      to: "pause.ts, handoff.ts, status.ts"
      via: "jsonl file state"
---

<objective>
Diagnose and fix all 41 pre-existing test failures across gateway, governance, escalation, and policy test suites. Target: >95% pass rate (555+/577 tests).
</objective>

<execution_context>
@/Users/terrenceyodaiken/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/__tests__/gateway/index.test.ts
@src/__tests__/governance/usage-tracker.test.ts
@src/__tests__/escalation/status.test.ts
@src/__tests__/policy/wiring.test.ts

## Known Failure Patterns (from previous runs)

### Gateway Tests (~7 failures)
- Session map isolation issues between tests
- Event processor mock state bleeding
- Thread isolation test failures
- Concurrent event handling failures

### Governance Tests (~24 failures)
- UsageTracker: File system state isolation (usage-tracker.test.ts)
- ModelRouter: Timeout issues (5+ second sleeps for model selection)
- BudgetEngine: Threshold/warning state detection
- Watchdog: Pattern detection and stats collection

### Telemetry Tests (~4 failures)
- 5000ms timeout suggests external dependency or timeout threshold

### Escalation/Policy Tests (~6 failures)
- Audit log length assertions failing
- Status/policy wiring detection issues

### Retry Queue (~1 failure)
- Event rebuild from event log timing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose gateway test failures</name>
  <files>src/__tests__/gateway/index.test.ts</files>
  <action>
**Run gateway tests individually to isolate failures:**
```bash
cd /Users/terrenceyodaiken/claude-workspace/moazbuilds-claudeclaw
bun test src/__tests__/gateway/index.test.ts 2>&1
```

**For each failure, identify root cause:**
- Session map state bleeding between tests
- Mock not properly reset
- Async timing issues

**Fix patterns to apply:**
1. Add `beforeEach` cleanup of session map state
2. Ensure gateway instance reset between tests
3. Fix `should create separate mappings for different threads` - likely parallel execution issue
4. Fix `should handle concurrent events consistently` - Promise.all ordering issue

**Specific fixes:**
- In `beforeEach` or `afterEach`, call `sessionMap.clear()` or reset function
- For concurrent tests, ensure proper serialization or use test-specific channels
- For thread isolation, verify event.channelId includes thread context
</action>
  <verify>bun test src/__tests__/gateway/index.test.ts 2>&1 | tail -20</verify>
  <done>Gateway tests: 19/19 or similar passing (baseline was 12/19)</done>
</task>

<task type="auto">
  <name>Task 2: Fix governance usage tracker tests</name>
  <files>src/__tests__/governance/usage-tracker.test.ts</files>
  <action>
**Run usage tracker tests to see specific failures:**
```bash
bun test src/__tests__/governance/usage-tracker.test.ts 2>&1
```

**Common issues with usage tracker tests:**
- File system state from previous tests bleeding into current test
- Shared `.claude/claudeclaw/usage/` directory not isolated
- JSON parse/stringify round-trip issues

**Fix approach:**
1. Add `beforeEach` to clean usage directory: `rm -rf .claude/claudeclaw/usage/*`
2. Add `afterEach` cleanup to prevent state bleeding
3. Ensure each test uses unique sessionId/channelId values
4. Check mock expectations match actual API responses

**If tests still fail after cleanup:**
- Verify `usage-tracker.ts` correctly handles empty/missing files
- Check that `getSessionUsage`, `getChannelUsage` handle null returns properly
- Ensure aggregate computation handles zero values
</action>
  <verify>bun test src/__tests__/governance/usage-tracker.test.ts 2>&1 | tail -20</verify>
  <done>Usage tracker tests: all passing (was ~6 failures)</done>
</task>

<task type="auto">
  <name>Task 3: Fix governance watchdog and model router tests</name>
  <files>src/__tests__/governance/watchdog.test.ts, src/__tests__/governance/model-router.test.ts</files>
  <action>
**Run watchdog tests:**
```bash
bun test src/__tests__/governance/watchdog.test.ts 2>&1
```

**Watchdog issues:**
- Stats collection returns empty array - likely `getStats()` not returning expected structure
- Pattern detection `expect().toHaveLength()` failing

**Fix approach:**
1. Verify `Watchdog.getStats()` returns proper structure with `toolCalls`, `turns`, etc.
2. Check `detectRepeatedToolPatterns()` returns correct array format
3. Ensure watchdog state is properly reset in `beforeEach`

**Run model router tests:**
```bash
bun test src/__tests__/governance/model-router.test.ts 2>&1
```

**Model router issues:**
- 5000ms timeout suggests actual LLM API being called instead of mocked
- `selectModel` taking too long

**Fix approach:**
1. Verify mocks are properly set up for `selectModel`
2. Check `GovernanceClient` mock returns immediately, not calling real API
3. Reduce or eliminate sleep-based timing in tests
</action>
  <verify>bun test src/__tests__/governance/watchdog.test.ts src/__tests__/governance/model-router.test.ts 2>&1 | tail -30</verify>
  <done>Watchdog and model router tests: all passing (was ~10 failures)</done>
</task>

<task type="auto">
  <name>Task 4: Fix escalation and policy tests</name>
  <files>src/__tests__/escalation/*.test.ts, src/__tests__/policy/*.test.ts</files>
  <action>
**Run escalation tests:**
```bash
bun test src/__tests__/escalation/ 2>&1
```

**Escalation issues:**
- `status.test.ts`: `expect().toHaveLength()` failing on getStats
- `notifications.test.ts`: Likely state isolation
- `pause.test.ts`, `handoff.test.ts`: JSONL file state bleeding

**Fix approach:**
1. Add `beforeEach` cleanup for pause queue, handoff files, notifications
2. Verify `getStats()` returns correct array structure
3. Check JSONL file paths are test-specific or properly cleaned

**Run policy tests:**
```bash
bun test src/__tests__/policy/ 2>&1
```

**Policy issues:**
- `wiring.test.ts`: GovernanceClient detection failing
- `audit-log.test.ts`: Length assertions on log entries
- `engine.test.ts`, `channel-policies.test.ts`, etc.: Likely isolated

**Fix approach:**
1. For `wiring.test.ts`: Verify mock `GovernanceClient` properly implements interface
2. For `audit-log.test.ts`: Check entry counting logic - may need `toBeGreaterThan` instead of `toHaveLength`
3. Ensure audit log cleanup between tests
</action>
  <verify>bun test src/__tests__/escalation/ src/__tests__/policy/ 2>&1 | tail -30</verify>
  <done>Escalation and policy tests: all passing (was ~10 failures)</done>
</task>

<task type="auto">
  <name>Task 5: Fix remaining test failures and verify >95% pass rate</name>
  <files>src/__tests__/retry-queue.test.ts, src/__tests__/event-processor.test.ts</files>
  <action>
**Run all tests and count failures:**
```bash
bun test 2>&1 | tail -50
```

**Focus on remaining failures:**
1. `retry-queue.test.ts`: "should rebuild from event log" - timing/state issue
2. Any remaining gateway, governance, escalation, policy failures

**Fix approach:**
1. For retry queue: Ensure event log is properly set up before rebuild test
2. Check `event-processor.test.ts` for similar isolation issues

**Final verification:**
- Count total tests and pass/fail ratio
- Target: 555+/577 = >96% pass rate
- If still below 95%, diagnose worst offenders and fix
</action>
  <verify>bun test 2>&1 | grep -E "^\d+ (pass|fail)" | tail -5</verify>
  <done>>95% test pass rate achieved (555+/577)</done>
</task>

</tasks>

<verification>
1. `bun test src/__tests__/gateway/ | grep -E "pass|fail"` shows all passing
2. `bun test src/__tests__/governance/ | grep -E "pass|fail"` shows all passing
3. `bun test src/__tests__/escalation/ src/__tests__/policy/ | grep -E "pass|fail"` shows all passing
4. Overall: `bun test 2>&1 | tail -10` shows 555+ tests passing
</verification>

<success_criteria>
- [ ] Gateway tests: 19/19 passing (baseline ~12/19)
- [ ] Governance tests: all passing (baseline ~12/24 failing)
- [ ] Escalation tests: all passing (baseline ~6 failing)
- [ ] Policy tests: all passing
- [ ] Overall: 555+/577 tests passing (>95%)
</success_criteria>

<output>
After completion, create `.planning/phases/15-test-fix-and-simplification/15-01-SUMMARY.md`
</output>
