---
phase: 15-test-fix-and-simplification
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/**/*.ts
  - src/__tests__/**/*.ts
autonomous: true
requirements:
  - SIMP-01: Code simplified with clear, explicit patterns
  - SIMP-02: Nested ternaries converted to if/else or switch
  - SIMP-03: Overly compact code broken into clear steps
  - SIMP-04: Redundant abstractions removed
  - SIMP-05: Project standards consistently applied
  - SIMP-06: All simplifications preserve exact functionality

must_haves:
  truths:
    - "No nested ternary operators remain in codebase"
    - "Complex chains broken into named intermediate variables"
    - "Redundant helper functions inlined or removed"
    - "All files follow project coding standards from CLAUDE.md"
    - "All simplifications verified by passing tests"
  artifacts:
    - path: "src/commands/telegram.ts"
      provides: "Simplified command handlers"
    - path: "src/commands/discord.ts"
      provides: "Simplified discord handlers"
    - path: "src/gateway/index.ts"
      provides: "Simplified gateway orchestrator"
    - path: "src/governance/*.ts"
      provides: "Simplified governance modules"
    - path: "src/escalation/*.ts"
      provides: "Simplified escalation modules"
    - path: "src/orchestrator/*.ts"
      provides: "Simplified orchestrator modules"
  key_links:
    - from: "Code simplification"
      to: "Test suite"
      via: "bun test verification"
---

<objective>
Apply code simplification across the entire codebase using project standards. Focus on recently modified files and high-complexity modules. Preserve exact functionality.
</objective>

<execution_context>
@/Users/terrenceyodaiken/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/commands/telegram.ts
@src/commands/discord.ts
@src/gateway/index.ts
@src/governance/usage-tracker.ts
@src/governance/budget-engine.ts
@src/governance/watchdog.ts
@src/governance/model-router.ts
@src/escalation/pause.ts
@src/escalation/handoff.ts
@src/escalation/notifications.ts
@src/escalation/status.ts
@src/orchestrator/executor.ts
@src/orchestrator/task-graph.ts
@src/orchestrator/workflow-state.ts
@src/policy/engine.ts
@src/policy/channel-policies.ts
@src/policy/skill-overlays.ts
@src/policy/approval-queue.ts
@src/policy/audit-log.ts

## Simplification Priorities

### High Priority (Recently modified, complex)
1. `src/commands/telegram.ts` - 1093 lines, complex message handling
2. `src/commands/discord.ts` - 1026 lines, complex gateway logic
3. `src/gateway/index.ts` - Gateway orchestrator

### Medium Priority (Core modules)
4. `src/governance/*.ts` - Usage tracking, budget, watchdog, model router
5. `src/escalation/*.ts` - Pause, handoff, notifications, status
6. `src/orchestrator/*.ts` - Executor, task graph, workflow state

### Lower Priority (Smaller, simpler)
7. `src/policy/*.ts` - Engine, policies, approvals, audit
8. Other utility modules
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify telegram.ts command handlers</name>
  <files>src/commands/telegram.ts</files>
  <action>
**Simplify telegram.ts focusing on:**

1. **Nested ternaries** - Find and convert to if/else chains:
   ```typescript
   // BEFORE
   const status = isLoading ? 'loading' : hasError ? 'error' : isComplete ? 'complete' : 'idle';
   
   // AFTER
   function getStatus(): string {
     if (isLoading) return 'loading';
     if (hasError) return 'error';
     if (isComplete) return 'complete';
     return 'idle';
   }
   ```

2. **Overly compact chains** - Break into named steps:
   ```typescript
   // BEFORE
   const result = items.filter(x => x.active).map(x => x.value).reduce((a, b) => a + b, 0);
   
   // AFTER
   const activeItems = items.filter(x => x.active);
   const values = activeItems.map(x => x.value);
   const sum = values.reduce((a, b) => a + b, 0);
   ```

3. **Complex conditionals** - Extract to named functions with clear names

4. **Redundant abstractions** - Remove one-liner functions that add no value

**Key areas to simplify:**
- `groupTriggerReason()` - complex nested if/else
- `handleMessage()` - long function, extract parts
- `extractTelegramCommand()` - simplify parsing
- `extractReactionDirective()` - simplify regex chains
</action>
  <verify>bun test src/__tests__/gateway/ 2>&1 | tail -5</verify>
  <done>Telegram.ts simplified, tests still passing</done>
</task>

<task type="auto">
  <name>Task 2: Simplify discord.ts gateway logic</name>
  <files>src/commands/discord.ts</files>
  <action>
**Simplify discord.ts focusing on:**

1. **Gateway dispatch handling** - Simplify `handleDispatch` switch statement
2. **Rate limit handling** - Extract to clear helper functions
3. **Message handling** - Break up `handleMessageCreate` long function
4. **Guild trigger logic** - Simplify `guildTriggerReason()` nested conditions

**Key areas to simplify:**
- `handleGatewayPayload` - complex nested switch
- `handleDispatch` - long switch with multiple cases
- `guildTriggerReason()` - deeply nested if/else
- `handleInteractionCreate` - long function with multiple interaction types
</action>
  <verify>bun test src/__tests__/gateway/ 2>&1 | tail -5</verify>
  <done>Discord.ts simplified, tests still passing</done>
</task>

<task type="auto">
  <name>Task 3: Simplify governance modules</name>
  <files>src/governance/usage-tracker.ts, src/governance/budget-engine.ts, src/governance/watchdog.ts, src/governance/model-router.ts</files>
  <action>
**Simplify governance modules:**

1. **usage-tracker.ts**:
   - Simplify file reading/writing patterns
   - Extract aggregation logic to clear named functions
   - Simplify null/undefined handling

2. **budget-engine.ts**:
   - Simplify `evaluate()` nested conditionals
   - Extract threshold checking to clear functions
   - Simplify state transitions (warn → degrade → block)

3. **watchdog.ts**:
   - Simplify pattern detection logic
   - Extract stats collection to clear functions
   - Simplify limit checking

4. **model-router.ts**:
   - Simplify provider/model selection chains
   - Extract fallback logic to named functions
   - Simplify capability mapping lookups
</action>
  <verify>bun test src/__tests__/governance/ 2>&1 | tail -5</verify>
  <done>Governance modules simplified, tests still passing</done>
</task>

<task type="auto">
  <name>Task 4: Simplify escalation modules</name>
  <files>src/escalation/pause.ts, src/escalation/handoff.ts, src/escalation/notifications.ts, src/escalation/status.ts</files>
  <action>
**Simplify escalation modules:**

1. **pause.ts**:
   - Simplify mode checking (admission_only vs admission_and_scheduling)
   - Extract pause/resume logic to clear functions

2. **handoff.ts**:
   - Simplify handoff package creation
   - Extract context building to named functions

3. **notifications.ts**:
   - Simplify notification type dispatching
   - Extract rate limiting logic
   - Simplify deduplication checks

4. **status.ts**:
   - Simplify status aggregation
   - Extract JSONL reading to clear helpers
</action>
  <verify>bun test src/__tests__/escalation/ 2>&1 | tail -5</verify>
  <done>Escalation modules simplified, tests still passing</done>
</task>

<task type="auto">
  <name>Task 5: Simplify orchestrator modules</name>
  <files>src/orchestrator/executor.ts, src/orchestrator/task-graph.ts, src/orchestrator/workflow-state.ts, src/orchestrator/resumable-jobs.ts</files>
  <action>
**Simplify orchestrator modules:**

1. **executor.ts**:
   - Simplify task execution flow
   - Extract governance checks to clear functions
   - Simplify error handling chains

2. **task-graph.ts**:
   - Simplify topological sorting
   - Extract cycle detection to named function
   - Simplify ready-task identification

3. **workflow-state.ts**:
   - Simplify state persistence logic
   - Extract atomic write operations
   - Simplify crash recovery

4. **resumable-jobs.ts**:
   - Simplify job scheduling logic
   - Extract cron parsing to clear functions
   - Simplify restart recovery
</action>
  <verify>bun test src/__tests__/orchestrator/ 2>&1 | tail -5</verify>
  <done>Orchestrator modules simplified, tests still passing</done>
</task>

<task type="auto">
  <name>Task 6: Simplify remaining modules and verify full suite</name>
  <files>src/policy/*.ts, src/gateway/*.ts, src/event-log.ts, src/event-processor.ts, src/retry-queue.ts</files>
  <action>
**Simplify remaining modules:**

1. **policy/engine.ts**:
   - Simplify rule evaluation chains
   - Extract matching logic to named functions

2. **gateway/index.ts**:
   - Simplify event processing flow
   - Extract session mapping to clear functions

3. **event-log.ts**:
   - Simplify segment rotation logic
   - Extract sequence allocation

4. **event-processor.ts**:
   - Simplify event dispatching

**Final verification:**
```bash
bun test 2>&1 | tail -20
```

Target: All tests still passing after simplification
</action>
  <verify>bun test 2>&1 | tail -10</verify>
  <done>Full test suite passes after all simplifications</done>
</task>

</tasks>

<verification>
1. `bun test 2>&1` shows full suite passing
2. No new warnings or errors introduced
3. Code complexity reduced (measured by fewer nested ternaries, shorter functions)
</verification>

<success_criteria>
- [ ] telegram.ts: nested ternaries removed, complex chains broken
- [ ] discord.ts: gateway logic simplified, nested conditionals extracted
- [ ] governance modules: budget/watchdog/router logic simplified
- [ ] escalation modules: pause/handoff/notifications simplified
- [ ] orchestrator modules: executor/task-graph/state simplified
- [ ] policy modules: engine/policies simplified
- [ ] All tests pass after simplification
</success_criteria>

<output>
After completion, create `.planning/phases/15-test-fix-and-simplification/15-02-SUMMARY.md`
</output>
