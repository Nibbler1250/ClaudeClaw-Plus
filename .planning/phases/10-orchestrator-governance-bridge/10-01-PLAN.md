---
phase: 10-orchestrator-governance-bridge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/governance-adapter.ts
  - src/orchestrator/executor.ts
  - src/__tests__/orchestrator/governance-adapter.test.ts
autonomous: true
requirements:
  - orchestrator-governance-interface
  - governance-client-orchestrator-mismatch
  - orchestrator-governance-flow

must_haves:
  truths:
    - "Orchestrator can perform governance checks on tasks via adapter"
    - "checkPolicy translates executor action to tool policy evaluation"
    - "checkBudget evaluates session budget before task execution"
    - "Blocked tasks fail with GovernanceBlocked error type"
  artifacts:
    - path: src/orchestrator/governance-adapter.ts
      provides: Adapter implementing executor GovernanceClient interface
      exports: OrchestratorGovernanceAdapter class
    - path: src/__tests__/orchestrator/governance-adapter.test.ts
      provides: Unit tests for adapter behavior
  key_links:
    - from: src/orchestrator/governance-adapter.ts
      to: src/governance/client.ts
      via: RealGovernanceClient.evaluateToolRequest
      pattern: evaluateToolRequest
    - from: src/orchestrator/governance-adapter.ts
      to: src/governance/index.ts
      via: evaluateBudget
      pattern: evaluateBudget
    - from: src/orchestrator/executor.ts
      to: src/orchestrator/governance-adapter.ts
      via: setGovernanceClient(new OrchestratorGovernanceAdapter())
      pattern: GovernanceClient interface
---

<objective>
Bridge the interface mismatch between orchestrator's expected GovernanceClient interface and the actual GovernanceClient class by creating an adapter.

Purpose: Enable task-level governance enforcement in the orchestrator by adapting the real GovernanceClient to the executor's expected interface.

Output: OrchestratorGovernanceAdapter class that implements executor's GovernanceClient interface.
</objective>

<context>
@src/orchestrator/executor.ts (lines 77-122)
@src/governance/client.ts
@src/governance/index.ts
@src/governance/budget-engine.ts (evaluateBudget at line 443)
@.planning/phases/10-orchestrator-governance-bridge/10-RESEARCH.md

**Executor GovernanceClient interface (what we must adapt TO):**
```typescript
export interface GovernanceClient {
  checkPolicy(channelId: string, action: string): Promise<GovernanceCheck>;
  checkBudget(sessionId: string, action: string): Promise<GovernanceCheck>;
}

export interface GovernanceCheck {
  allowed: boolean;
  reason?: string;
  blockedBy?: string;
}
```

**Real GovernanceClient class (what we adapt FROM):**
```typescript
export class GovernanceClient {
  evaluateToolRequest(request: ToolRequestContext): PolicyDecision;
  async getBudgetState(channelId?: string);
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OrchestratorGovernanceAdapter</name>
  <files>
    - src/orchestrator/governance-adapter.ts
    - src/__tests__/orchestrator/governance-adapter.test.ts
  </files>
  <behavior>
    - Test 1: checkPolicy returns GovernanceCheck with allowed=true when policy allows
    - Test 2: checkPolicy returns GovernanceCheck with allowed=false and reason when policy denies
    - Test 3: checkBudget returns GovernanceCheck with allowed=true when budget OK
    - Test 4: checkBudget returns GovernanceCheck with allowed=false when budget exceeded
    - Test 5: Adapter wraps real GovernanceClient and delegates appropriately
  </behavior>
  <action>
    Create src/orchestrator/governance-adapter.ts with:

    1. Import GovernanceClient (as RealGovernanceClient) and getGovernanceClient from "../governance/client"
    2. Import evaluateBudget from "../governance" (re-exported from budget-engine)
    3. Import ToolRequestContext and PolicyDecision from "../policy/engine"
    4. Import GovernanceCheck and GovernanceClient interface from "./executor"

    5. Export OrchestratorGovernanceAdapter class that:
      - Has private field: private realClient: RealGovernanceClient
      - Constructor accepts optional RealGovernanceClient (defaults to getGovernanceClient())
      - checkPolicy(channelId, action): Promise<GovernanceCheck>:
        - Build ToolRequestContext with eventId=crypto.randomUUID(), source="orchestrator", channelId, toolName=action, toolArgs=undefined, timestamp
        - Call realClient.evaluateToolRequest(request) - NOTE: this is synchronous, wrap in Promise.resolve()
        - Map PolicyDecision.action "allow" -> allowed=true, "deny"/"require_approval" -> allowed=false
        - Return GovernanceCheck with allowed, reason, blockedBy (set blockedBy="policy" on deny)
      - checkBudget(sessionId, action): Promise<GovernanceCheck>:
        - Call evaluateBudget({ sessionId }) - NOTE: this is async
        - If any evaluation has state "block", return allowed=false with reason
        - Otherwise return allowed=true

    6. Keep the GovernanceClient interface import from executor for type checking

    IMPORTANT: 
    - evaluateToolRequest is sync but executor interface expects Promise, use Promise.resolve()
    - evaluateBudget is async so await it properly
    - Do NOT import or modify GovernanceCheck/GovernanceClient from executor - these are the TARGET interface we implement
  </action>
  <verify>
    <automated>bun test src/__tests__/orchestrator/governance-adapter.test.ts</automated>
  </verify>
  <done>
    OrchestratorGovernanceAdapter implements executor's GovernanceClient interface correctly.
    checkPolicy delegates to realClient.evaluateToolRequest.
    checkBudget delegates to evaluateBudget.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire adapter into executor</name>
  <files>src/orchestrator/executor.ts</files>
  <action>
    In src/orchestrator/executor.ts:

    1. Add import at top: import { OrchestratorGovernanceAdapter } from "./governance-adapter";

    2. Keep the local GovernanceClient interface (lines 77-80) - it defines the contract we implement

    3. Keep setGovernanceClient function as-is - it accepts GovernanceClient interface

    4. The executor is already wired to use governanceClient via checkGovernance function (lines 94-122)
       The adapter just needs to be instantiated and passed to setGovernanceClient() by the consumer

    NOTE: No changes needed to executor.ts itself if using the adapter pattern correctly.
    The caller (likely src/orchestrator/index.ts or main entry) will do:
      setGovernanceClient(new OrchestratorGovernanceAdapter());

    If there is no orchestrator index.ts that wires this up, add initialization there.
  </action>
  <verify>
    <automated>bun test src/__tests__/orchestrator/executor.test.ts</automated>
  </verify>
  <done>
    OrchestratorGovernanceAdapter can be passed to setGovernanceClient() and works with executor's checkGovernance function.
  </done>
</task>

</tasks>

<verification>
1. Adapter compiles without errors: bun build src/orchestrator/governance-adapter.ts
2. Adapter tests pass: bun test src/__tests__/orchestrator/governance-adapter.test.ts
3. Executor tests still pass: bun test src/__tests__/orchestrator/executor.test.ts
4. Integration: Adapter instance satisfies GovernanceClient interface from executor
</verification>

<success_criteria>
- OrchestratorGovernanceAdapter class exists in src/orchestrator/governance-adapter.ts
- Adapter implements checkPolicy(channelId, action): Promise<GovernanceCheck>
- Adapter implements checkBudget(sessionId, action): Promise<GovernanceCheck>
- checkPolicy delegates to realClient.evaluateToolRequest with proper ToolRequestContext
- checkBudget delegates to evaluateBudget with proper scope
- GovernanceCheck return type matches executor interface { allowed: boolean, reason?: string, blockedBy?: string }
- Unit tests verify adapter behavior with mocked dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/10-orchestrator-governance-bridge/10-01-SUMMARY.md`
</output>
